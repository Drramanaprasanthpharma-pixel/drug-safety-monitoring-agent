# Local development & testing only — not installed in the deployed Vercel
# function. Install with: pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt
uvicorn[standard]==0.30.6
pytest==8.3.3
# httpx is also in requirements.txt now (used at runtime by agent/ai_client.py);
# FastAPI's TestClient uses it too, so no separate dev-only pin is needed here.
